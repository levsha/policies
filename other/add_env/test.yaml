name: add-env
policies:
  - add_env.yaml
resources:
  - resource.yaml
results:
  - policy: add-env
    rule: add-env
    resource: myapp-pod
    patchedResource: patchedResource.yaml
    kind: Pod
    namespace: default
    result: pass
